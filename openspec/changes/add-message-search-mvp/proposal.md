## Why

成熟 IM 需要在会话内快速查找历史文本消息。当前已有历史消息分页和重连补偿，但缺少面向用户的消息搜索入口。

## What Changes

- 新增房间内文本消息搜索。
- 权限沿用房间成员校验。
- 搜索先使用数据库 `LIKE` MVP，不引入 Elasticsearch。
- 仅返回正常状态文本消息，排除撤回、删除和非文本消息。

## Non-Goals

- 不做全文索引、高亮、复杂排序或跨房间搜索。
- 不搜索图片、文件 JSON 扩展内容。
