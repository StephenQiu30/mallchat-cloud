## 1. 产品编排

- [x] 1.1 调研 `demo.html` 与当前后端 chat specs。
- [x] 1.2 编写 IM MVP PRD。
- [x] 1.3 编写 OpenSpec 执行计划。

## 2. OpenSpec 认领

- [x] 2.1 创建 `orchestrate-im-product-mvp` change。
- [x] 2.2 新增 `im-product-mvp` spec delta。
- [x] 2.3 明确本轮无后端运行时代码改动。

## 3. 验证

- [x] 3.1 执行 `openspec validate orchestrate-im-product-mvp --strict`。
- [x] 3.2 执行 `mvn -B -DskipTests compile`。
- [x] 3.3 将测试结论写入 `docs/acceptance`。
