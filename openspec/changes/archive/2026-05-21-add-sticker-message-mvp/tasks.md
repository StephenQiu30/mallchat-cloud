## 1. Tests First

- [x] 1.1 `ChatMessageHelperTest` 覆盖合法/非法贴纸 extra。
- [x] 1.2 `ChatSessionServiceImplTest` 覆盖贴纸会话预览。

## 2. Implementation

- [x] 2.1 扩展 `ChatMessageTypeEnum` 与 `ChatMessageHelper`。
- [x] 2.2 同步 DTO/Swagger 文案。

## 3. Validation

- [x] 3.1 focused Maven tests 通过。
- [x] 3.2 `openspec validate --all --strict` 通过。
