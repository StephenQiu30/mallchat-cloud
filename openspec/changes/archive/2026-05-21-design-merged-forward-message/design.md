# 合并转发后端骨架设计

## Decision

m8 不新增合并转发表，也不引入消息包详情页。先完成单条转发 MVP，合并转发保留为后续 change。

## Future Minimal Contract

后续实现时优先复用 `ChatMessage`：

- 合并转发本身作为一种消息类型。
- `extra` 保存标题、摘要和消息快照 ID 列表或快照摘要。
- 服务端创建合并转发前校验所有来源消息均属于当前用户可访问房间。
- 已撤回、删除或跨权限消息不进入快照。

## Deferred

- 大体量快照存储。
- 详情页分页。
- 长图导出。
- 跨端 UI 渲染。
