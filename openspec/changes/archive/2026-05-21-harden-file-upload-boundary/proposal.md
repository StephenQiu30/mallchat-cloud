## Why

MallChat 文件服务是聊天图片、聊天文件和用户头像的入口。当前上传只校验业务类型，缺少文件名、大小、类型和基础图片内容边界，生产环境容易接受非法文件。

## What Changes

- 增加轻量文件上传校验器。
- 按业务类型限制最大大小、后缀和 Content-Type。
- 对头像和聊天图片做最小图片魔数校验。
- 拒绝空文件、危险文件名、无后缀和不允许的文件类型。

## Non-Goals

- 不建设内容审核系统。
- 不引入杀毒扫描或异步转码。
- 不改变 COS key 的随机路径策略。
