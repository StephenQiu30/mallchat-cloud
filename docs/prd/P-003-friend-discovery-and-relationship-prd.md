---
layer: PRD
doc_no: "P-003"
audience:
  - PM
  - Dev
  - QA
  - Ops
feature_area: friend-discovery-and-relationship
purpose: "定义 QQ-like 添加好友、好友发现、关系维护和好友资料展示的后端增强边界。"
canonical_path: "docs/prd/P-003-friend-discovery-and-relationship-prd.md"
status: draft
version: "0.1.1"
owner: "StephenQiu30"
inputs:
  - "docs/prd/P-001-im-real-time-communication-prd.md"
  - "openspec/specs/chat-friend/spec.md"
outputs:
  - "好友发现与关系增强需求"
  - "后续 OpenSpec change: enhance-friend-discovery-relationship"
triggers:
  - "新增添加好友入口"
  - "调整好友申请、搜索、备注、拉黑或删除关系"
downstream:
  - "docs/design/"
  - "docs/acceptance/"
---

# 好友发现与关系 PRD

## 1. 背景

后端已有好友申请、同意/忽略、好友列表、双向好友关系、移除好友和私聊房间初始化能力。`UserController` 也提供用户分页搜索，可作为添加好友前的用户发现基础。

但 QQ-like IM 不只需要“能加好友”，还需要搜索发现、申请状态可见、备注分组、删除/拉黑、隐私限制和共同好友等关系体验。当前代码没有看到备注、分组、黑名单、来源渠道、申请过期和推荐好友模型。

## 2. 产品目标

1. 首版形成“搜索用户 -> 发起申请 -> 对方处理 -> 好友列表 -> 私聊房间”的完整闭环。
2. P1 支持好友备注、分组、删除关系后的私聊权限收敛。
3. P2 支持拉黑、隐私校验、共同好友和好友推荐。

```gherkin
Given 用户 A 搜索到用户 B
When 用户 A 发送好友申请且用户 B 同意
Then 系统应建立双向好友关系
And 返回或创建稳定私聊房间
```

## 3. 非目标

- 不在首版实现复杂通讯录导入、手机号匹配和推荐算法。
- 不把好友关系与关注、粉丝、动态订阅混为一套关系。
- 不破坏现有 `user_friend` 和 `user_friend_apply` 的基础模型。

## 4. 核心用户故事

### 4.1 添加好友用户

作为用户，我希望可以通过昵称、账号或手机号搜索用户并发送好友申请。

验收标准：
- 搜索结果应脱敏展示头像、昵称、简介、在线状态和当前关系状态。
- 重复申请、互相申请、已经是好友、添加自己均应有明确错误或幂等结果。
- 好友申请消息长度和敏感内容有基础校验。

### 4.2 处理申请用户

作为收到申请的用户，我希望可以查看申请列表并同意或忽略。

验收标准：
- 待处理申请只允许目标用户处理。
- 同意申请后创建双向好友关系与稳定私聊房间。
- 忽略申请不创建好友关系。

### 4.3 好友列表用户

作为聊天用户，我希望好友列表展示在线状态、备注、分组和最近关系状态。

验收标准：
- MVP 展示头像、昵称、在线状态。
- P1 增加好友备注、分组排序、关系状态。
- 删除好友后，应阻止继续发送私聊消息。

### 4.4 好友发现接口用户

作为前端接口调用方，我希望好友发现接口直接返回关系状态，避免多端重复拼接和推导。

验收标准：
- `/chat/friend/search` 复用用户分页搜索能力，由后端统一返回 `friendStatus`。
- `friendStatus` 值域固定为：`0` 陌生人、`1` 本人、`2` 已是好友、`3` 我已发出待处理申请、`4` 对方已发出待处理申请。
- 搜索应优先通过用户服务的 `notId` 排除当前用户；如果兼容场景仍返回本人，应保持 `friendStatus=1`。
- `current <= 0` 应归一为第一页；`pageSize` 只允许在 `1-20` 范围内，避免大页查询。
- `/chat/friend/delete` 应具备幂等性，方便客户端重复点击或网络重试。

## 5. 数据与权限边界

- `user_friend` 是双向好友事实来源。
- `user_friend_apply` 是申请流程事实来源。
- 好友搜索复用用户服务，但只能返回脱敏信息。
- 私聊消息发送必须继续依赖 `isMutualFriend` 校验。
- 好友发现优先复用 `UserFeignClient#listUserByPage`，不新增并行用户搜索边界。
- 关系状态只读取 `UserFriend`、`UserFriendApply` 和在线状态，不改写申请审批模型。
- 继续保持 `/chat/friend/add` 禁用，新增好友必须走申请审批链路。

## 6. 首版验收门禁

- 好友申请、重复申请、同意、忽略、删除好友、私聊权限至少有单元测试或集成测试覆盖。
- 好友发现接口必须限制分页大小与脱敏字段。
- 好友关系变更必须能触发实时通知或会话更新。
- chat-service 相关聚焦测试应在 Maven reactor 中执行，例如使用 `-pl :mallchat-chat-service -am`，确保 common/cache 等依赖模块参与构建。

## 7. 风险与边界

- 好友删除后是否保留历史私聊房间和会话，需要产品确认。
- 拉黑与删除不是同一功能，不能用删除关系替代拉黑。
- 当前首版以 service-first 测试兜底，后续如扩大范围，应补充 controller 层契约测试。
- Redis 缓存残留但数据库关系已失效的降级场景，需要在后续可靠性批次补充测试。

## 8. 待确认问题

- 用户是否需要唯一 MallChat 号？
- 好友备注、分组和黑名单是否进入 P1？
- 申请被忽略后是否允许再次申请，冷却期多长？

## 9. 变更记录

| 日期 | 作者 | 版本 | 变更说明 |
| --- | --- | --- | --- |
| 2026-05-19 | StephenQiu30 | 0.1.0 | 初始化好友发现与关系增强 PRD |
| 2026-05-21 | StephenQiu30 | 0.1.1 | 合并根目录过程文档中的长期有效接口约束与验收边界 |
